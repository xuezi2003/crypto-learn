# Cryptography - Knowledge Base 归档与修复说明

## 基本信息

- **归档时间**：2026-07-31
- **来源知识库**：Crytography - Knowledge Base（飞书拼写如此，本地目录已修正为 Cryptography - Knowledge Base）
- **space_id**：`7122293244385312771`
- **抓取方式**：`lark-cli --as user`，`docs +fetch --doc-format markdown` 导出正文、`drive +download` 下载附件、`base record list` 导出多维表格
- **树规模**：96 个节点（docx 85、file/PDF 9、bitable 1、旧版 doc 1），全部抓取成功，无权限失败

## 归档结构约定

- 每个节点一个 `.md`；有子节点的章节建同名文件夹，子节点递归放入。
- 附件、画板导出图放各章 `assets/` 子目录，正文用相对路径引用，飞书原始链接以 HTML 注释保留。
- 文件名不使用全角字符：`：`→` - `、`／`→`-`、`？`→`?`。

## 特殊处理

1. **07_Benchmark of Cryptographic Primitives**：旧版 `doc` 类型，API 不支持转 Markdown，正文导出为 PDF（同目录 `.pdf`），`.md` 为说明。
2. **知识库建设 TODO list**（bitable）：导出为 Markdown 表格，表头字段名已中文化（多行文本→任务、单选→状态），空记录行已删除。
3. **首页.md**：飞书默认模板（无实际内容），已改写为本地章节导航页。

## 排版修复记录（2026-07-31）

导出保真度缺陷的批量修复，按类别逐项 dry-run 验证后执行：

| # | 类别 | 处理 | 数量 |
|---|------|------|------|
| 1 | 图片 Alt 污染（`addCriterion` 等 OCR 垃圾） | Alt 统一替换为 `![图片]` | 277 处 / 46 文件 |
| 2 | `<title>` 标签残留 | 转为 `# 标题`；与正文重题的去重 | 30 文件 |
| 3 | LaTeX 双重转义 | 随 Alt 清洗一并消除，无残留 | — |
| 4 | 素材未本地化 | 8 个附件（PDF/PPTX/PPTM）curl 下载、6 个画板 `docs +media-download` 导出 jpg，标签替换为相对路径 | 14 处 |
| 5 | 同 URL 相邻碎链接 | 合并 | 4 对 |
| 6 | Unicode 字符公式 | 逐文件手工改写为 `$...$` LaTeX | 11 文件约 120 处 |
| 7 | 列表全部 `1.` 编号 | 逐文件判断，逻辑连续序列重编号 | 4 文件 |
| 8 | 结构标签残留 | `<sub-page-list>`→本地相对链接目录（4 文件）；`<grid>/<column>`→`![说明](url)`（3 处）；4 处 `<sheet>` 嵌入经 lark-sheets 导出为 Markdown 表格；零宽字符清除 | — |
| 9 | 标题跳级 | 降为上一级+1 | 18 处 |
| 10 | 空行/行尾空格/表格/typo | 3+ 空行压缩（103 处）、行尾空格（59 行）、PSI Protocols 表格列数与 `<br/>`、TODO list 表头与空行、EUROCPYRT→EUROCRYPT 等 | — |
| 11 | 文件名全角字符与命名不统一 | 28 项重命名 + 全库引用同步更新，死链检查 0 | — |
| 12 | 正文图片为飞书临时链接（已过期失效） | 从过期 URL 解出内嵌媒体 ID，经 lark-cli 重新 fetch 46 个文档取新鲜链接按 ID 精确对账（PIR-PSI 1 张 ID 转写错位按位置对齐），curl 下载 277 张至各章 `assets/images/`，引用改为相对路径、原 URL 以 HTML 注释保留 | 277 张 / 46 文件 |
| 13 | 分类条目写成有序列表（无标题层级） | 逐文件手工改为 `##`/`###` 标题：代码相关、测试数据集与测试工具（分类转标题）；安全与密码会议列表（H1 混用修正、粗体伪标题转 `###`）；ROM 定义（三类定义转 `###`）；00_科研入门（PDF 链接错位清理） | 5 文件 |

## 遗留问题（源文档层面，未改动）

- **空壳笔记**：`TEE.md`、`02_高效交流 Talk.md`（仅标题）、`06` 章 6 个 `TODO - ` 前缀文件（只有片段/References）、`Cryptographic Adversaries.md`（正文基本为空）——飞书原文即如此。
- `PIR-PSI.md` 以"（我先试着构造一下）"收尾，为作者笔记原样。
- `[KM22, Security]….md` 结尾有一段与论文无关的 TikTok 听证会感想，保留未删。
- 少量已有 `$...$` 公式内部仍含 Unicode 数学斜体字符（如 `$(𝑥_𝑖, 𝑟_𝑖)$`），可渲染但不够规范。
- `[BMX22, PETS]….md` 的正文含一个指向 `aminer.cn` 的损坏链接片段，保留原样。
