# 项目指南

## 项目概述

本库是个人密码学学习笔记库。内容来自三本教材的 PDF 与视频资料，经 OCR 或手工转写为 Markdown，按章节归档。

### 三本教材

1. **《Introduction to Modern Cryptography, 3rd Edition》**（Katz & Lindell，简称 **KL 教材**）：理论向，重定义与归约证明，英文原文笔记。
2. **《信息安全数学基础——算法、应用与实践（第2版）》**（任伟）：数学基础，算法与编程导向，涵盖群、环、域、数论、椭圆曲线。
3. **《现代密码学 第5版》**（杨波）：工程与应用导向，重算法与协议。

## 目录结构

```
crypto-learn/
├── 分章节/                  # 三本教材的按章笔记（含 OCR 提取的图片）
│   ├── Introduction_to_Modern_Cryptography_3rd_Edition/
│   │   ├── 两位数字前缀 + 章节名.md           # 正文，含附录/索引
│   │   ├── 翻译/Ch<NN>/                      # 译文（过长时拆分多部分）
│   │   ├── 习题/Ch<NN>/                      # 习题解答及配套脚本
│   │   └── assets/                           # 图片按章子目录存放
│   ├── 信息安全数学基础_算法应用与实践_第2版_任伟/   # 结构同上（无翻译/习题）
│   └── 现代密码学_第5版_杨波/                      # 结构同上（无翻译/习题）
├── 原文件/                  # 原始 PDF（pdf/）与全本 Markdown（md/，OCR 母本，禁止修改）
├── 汇报/                    # 阶段性汇报幻灯片（.pptx）
├── 补充资料/
│   ├── 网课/                 # 西电杨波第四版视频课笔记
│   ├── KL官方题解_第三版/    # KL 第三版官方教师题解的手工 Markdown 转写（Ch<NN>.md）
│   └── 飞书分享-Cryptography - Knowledge Base/   # 飞书同名知识库的 Markdown 归档
├── .temp/                   # 临时文件目录
├── AGENTS.md
└── CLAUDE.md
```

## 命名与归档约定

- 分章节文件命名：`两位数字前缀 + 章节名`。前缀仅用于排序，章节编号与教材一致。
- KL 教材译文放 `翻译/Ch<NN>/`，命名 `Ch<NN>_translation.md`。文件过长时拆分多部分（如 `Ch03_translation_1.md`）。译文**内嵌图片**：先把图片上传至私有图床（Lsky Pro，配置见 `/workspace/XueServer/docs/lsky-pro-guide.md` 与 `.env`），再以图床直链嵌入（如 `![Image](https://lsky.jerryxue.top/i/...)`）。
- KL 教材习题解答放 `习题/Ch<NN>/`，命名 `Ch<NN>_exercises_solutions.md`。配套 Python 脚本放同一文件夹，命名 `solve_ex<章号>_<题号>[_后缀].py`（如 `solve_ex1_1.py`）。
- 图片放各章 `assets/` 子目录。文中用相对路径引用：`./assets/<章>/...`。

## 技术栈

- 文档：Markdown + LaTeX 公式。
- 脚本：Python 3.12。统一使用虚拟环境 `.venv/`（`source .venv/bin/activate`）。新依赖只安装进 `.venv/`。
- 版本控制：Git，单分支 `main`。

## Markdown 格式约定

- 行内公式用 `$...$`，不跨行。行间公式用 `$$...$$`。定界符必须成对，单行书写（开闭界同行）亦可。公式编号写进公式内部：`\tag{2.1}`。
- 美元金额必须转义：`\$89.878`。防止 `a$b`、`$5` 被误识别为公式。
- 伴读回答：简洁，契合原文。引用原文（如"构造 3.17"）时，先搜索上下文核对，再作答。

## 提交信息风格（参考）

```
前缀: 一句话概括本次改动

- 详细说明 1
- 详细说明 2
```

常见前缀：`新增:`、`修正:`、`重构:`、`收尾:`，也可带书名或章节名（如 `Ch05修复:`）。标题用动宾结构，正文用 `- ` 逐条列出具体改动。

## 注意事项

1. **OCR 准确性**：修改数学定义、算法步骤、定理编号时，必须对照原 PDF 核实。
2. **PDF 转 Markdown**：用 PaddleOCR-VL-1.6（MCP）整本识别生成初稿，再手动修复 OCR 错字、公式、表格、`<div>` 残留。图片存对应 `assets/`。PDF 源文件保留。
3. **敏感文件**：密码、密钥、个人隐私文件不加入版本控制。
4. **临时产物**：一律放 `.temp/`。`__pycache__/`、解包检查目录等无保留价值的产物用完即删，不提交。正式产出（译文、习题解答及脚本、图片图表等）及时归档到对应目录，`.temp/` 不留副本。
